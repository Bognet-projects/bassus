{% extends "base.html.twig" %}

{% block body %}
    <main>
        <div class="container">
            <div class="card">
                <div class="card-header">
                    Termék #{{ product.id }}
                </div>
                <div class="card-body">
                    {{ form(form) }}
                </div>
                <a href="{{ path('profile') }}" class="btn btn-secondary">Back</a>
            </div>
        </div>
    </main>
{% endblock %}